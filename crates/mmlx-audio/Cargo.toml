[package]
name = "mmlx-audio"
version = "0.1.0"
edition = "2021"

[features]
default = []
audio = ["dep:cpal", "dep:anyhow"]

[dependencies]
mmlx-core = { workspace = true }
mmlx-synth = { workspace = true }
log = "0.4"
cpal = { version = "0.15.3", optional = true }
anyhow = { version = "1.0", optional = true }

[dev-dependencies]
mmlx-core = { workspace = true }
